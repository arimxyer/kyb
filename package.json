{
  "name": "know-your-ballot",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.14",
    "node": ">=22.13.0"
  },
  "scripts": {
    "install:ci": "bun install --frozen-lockfile",
    "dev": "bun run dev:local",
    "dev:local": "bun scripts/convex-target-guard.mjs local && next dev",
    "dev:integration": "bun scripts/convex-target-guard.mjs integration && next dev",
    "build": "next build",
    "build:worker": "bun scripts/convex-target-guard.mjs non-production && opennextjs-cloudflare build",
    "start": "bun scripts/convex-target-guard.mjs non-production && next start",
    "preview": "bun run preview:local",
    "preview:local": "bun scripts/convex-target-guard.mjs local && opennextjs-cloudflare build && opennextjs-cloudflare preview",
    "preview:integration": "bun scripts/convex-target-guard.mjs integration && opennextjs-cloudflare build && opennextjs-cloudflare preview",
    "deploy:production": "bun scripts/convex-target-guard.mjs production-ci && opennextjs-cloudflare build && opennextjs-cloudflare deploy",
    "fixtures:reset:local": "bun scripts/reset-local-fixtures.mjs",
    "fixtures:seed:local": "bun scripts/convex-target-guard.mjs local && bunx convex run seed:prototype '{}' --deployment local",
    "test:e2e:prepare": "CONVEX_AGENT_MODE=anonymous bunx convex dev --once --typecheck enable --tail-logs disable && bun run fixtures:reset:local",
    "test:e2e:local": "bun run test:e2e:prepare && bun run build && bunx playwright test",
    "test:e2e:worker": "bun run test:e2e:prepare && bun run build:worker && PLAYWRIGHT_TARGET=worker bunx playwright test --grep @smoke --project=chromium-desktop",
    "cf-typegen": "wrangler types --env-interface CloudflareEnv ./cloudflare-env.d.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs",
    "lint": "oxlint --type-aware --deny-warnings ."
  },
  "dependencies": {
    "@base-ui/react": "^1.6.0",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "convex": "^1.42.3",
    "lucide-react": "^1.26.0",
    "next": "16.2.11",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "shadcn": "^4.14.1",
    "tailwind-merge": "^3.6.0",
    "tw-animate-css": "^1.4.0"
  },
  "devDependencies": {
    "@opennextjs/cloudflare": "1.20.2",
    "@playwright/test": "1.61.0",
    "@tailwindcss/postcss": "4.3.3",
    "@types/node": "26.1.1",
    "@types/react": "19.2.17",
    "@types/react-dom": "19.2.3",
    "oxlint": "1.75.0",
    "oxlint-tsgolint": "7.0.2001",
    "tailwindcss": "4.3.3",
    "typescript": "6.0.3",
    "wrangler": "4.114.0"
  },
  "type": "module",
  "displayName": "Know Your Ballot"
}
